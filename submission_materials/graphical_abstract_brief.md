# Graphical Abstract Brief

Recommended graphical abstract direction:

Use a simplified one-panel version of the current method figure, centered on the journal-facing message:

`UAV RGB + infrared frames -> pairwise homography evidence -> robust scene consensus -> QA-guided retained scene transform -> risk-coverage operating profile`

Design constraints:

- Use "infrared-visible" as the main terminology.
- Show UAV-TAlign-12K as the benchmark input and UAV-TAlign as the scene-level reliability framework.
- Emphasize retained scene-level alignment products rather than raw pairwise matching only.
- Avoid dense tables, internal threshold names, long hashes, and implementation details.
- Reuse verified visual assets only; do not introduce unverified rectified before/after examples.

Candidate source figure:

- `figures/fig_method_pipeline.pdf`

Suggested final asset:

- `graphical_abstract.pdf`
- landscape aspect ratio
- minimal text, high contrast, readable at thumbnail size
